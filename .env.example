# Copy this file to .env and fill in the values. Never commit .env.
# Non-secret connection details (server, database, account, warehouse, role)
# live in pipeline/config.yaml — only credentials belong here.

# Azure SQL source
AZURE_SQL_USER=
AZURE_SQL_PASSWORD=

# Snowflake target
SNOWFLAKE_USER=
SNOWFLAKE_PASSWORD=
